{ "applyTo": ["**/*.{tsx,jsx,svelte,vue,component.ts,component.html,erb,slim,haml,css,scss,sass,less}"], "body": [ "Structure: separate presentation from logic (components + hooks/stores/services/controllers).", "Naming: use descriptive component/function names; avoid abbreviations; reveal intent (e.g., UserProfileCard not UPC).", "Accessibility: semantic elements; proper ARIA labels; focus handling for dialogs/modals; avoid color-only cues; ensure keyboard navigation.", "Styles: keep logic and styles separate; scope selectors appropriately; use tokens/variables/design system; avoid inline styles and IDs; prefer low specificity.", "Framework-Specific Lifecycle:", "  - React: avoid heavy work in render; split large components; correct list keys; no blocking handlers.", "  - Svelte: leverage reactive statements; proper component lifecycle; avoid unnecessary stores.", "  - Angular: OnPush change detection where possible; unsubscribe observables; avoid logic in templates.", "  - Rails: keep views thin; extract helpers/partials; use content_for for layout sections.", "  - Ionic: follow platform UI guidelines; optimize for mobile performance; test on multiple screen sizes.", "Logic: use guard clauses; prefer clean ternaries; short-circuit when clearer (&&, ??, ||); handle edge cases explicitly; extract complex conditions into named functions.", "Functions: max 20 lines; single purpose; ≤3 parameters; pure when possible; avoid side effects in render/template logic.", "DRY: extract repeated markup/logic into reusable components; create custom hooks/composables for shared state logic.", "Testing approach by framework:", "  - React: RTL with getByTestId", "  - Svelte/Vue: @testing-library/svelte or @testing-library/vue", "  - Angular: TestBed with fixture.debugElement queries", "  - Rails: Capybara for integration, RSpec for units", "  - Ionic: Jasmine/Karma with platform mocks", "Test coverage: render/response, main interactions, edge states (loading/error/empty); keep tests deterministic and fast.", "When changing existing UI, propose quick perf/a11y checks (re-render/query hotspots, keyboard flow, color contrast, responsive behavior)." ] }